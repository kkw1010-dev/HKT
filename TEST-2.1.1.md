# CIGAR 2.1.1 게임 테스트 지시서

작성 2026-09-26. 로드 오더에는 `+CIGAR 2.1.1`이 켜져 있습니다. MO2는 꺼 둔 상태이니 직접 켜서
게임을 실행하세요. DLL만 바뀌었으므로 기존 세이브로 해도 됩니다.

## 이번에 바뀐 것

- **주시하기 1인칭 줌.** 1인칭에서는 손 모델 시야각만 바뀌어서 화면이 줌되지 않았습니다.
  넥서스 댓글 두 건의 원인입니다. 이제 1인칭과 3인칭 모두 화면이 줌됩니다.
- **패드로 시간 보내기.** 패드로 누르면 0.3초 만에 멈췄습니다. 이제 누르는 동안 계속됩니다.
- **FOV 덮어쓰기 경고.** 다른 모드가 카메라 시야각을 덮어쓰면, 게임 안에 알림이 한 번 뜹니다.

## 테스트 순서

기본 프롬프트 키는 키보드 1–4입니다. 패드 버튼은 SkyPrompt 기본값(Xbox 기준 B, X, Y, A)이며,
프롬프트에 표시된 버튼을 누르면 됩니다.

### 1. 1인칭 주시하기 (키보드)

1. `F`를 눌러 1인칭으로 바꿉니다.
2. 무기를 넣고, 먼 풍경을 보며 5초 동안 가만히 섭니다.
3. **주시하기 (누르고 있기)** 가 뜨면 그 키를 누르고 있습니다.

- 기대: 화면이 2초에 걸쳐 확대되고, 키를 떼면 돌아옵니다.
- 사람을 조준하고 해도 됩니다. 그러면 프롬프트에 그 사람 이름이 붙습니다.

### 2. 3인칭 주시하기 (키보드)

1번을 3인칭으로 한 번 더 합니다.

- 기대: 전처럼 확대됩니다.

### 3. 패드로 주시하기

1번 또는 2번을 패드 버튼으로 합니다.

- 기대: 키보드와 같습니다.

### 4. 패드로 시간 보내기

1. 무기를 넣고 바닥을 내려다보며 가만히 서서 **앉기**를 누릅니다. 또는 의자에 앉습니다.
2. **시간 보내기 (누르고 있기)** 가 뜨면 패드 버튼을 몇 초 동안 누르고 있습니다.

- 기대: 누르는 동안 시간이 점점 빨리 흐르고, 떼면 멈춥니다.
- 고치기 전에는 0.3초 만에 멈췄습니다.

### 5. 키보드로 시간 보내기

4번을 키보드로 한 번 더 합니다.

- 기대: 전과 같이 누르는 동안 흐르고, 떼면 멈춥니다.

### 6. 패드로 다른 프롬프트 한두 개

예시는 투구입니다. 투구가 없으면 콘솔에서 받습니다.

```
player.additem 00012E4D 1
```

"북부 철 투구"를 쓰고 던전 밖에 서 있으면 **투구 벗기 (길게)** 가 뜹니다. 패드로 길게 누릅니다.

- 기대: 키보드와 똑같이 벗겨집니다.

## 끝나면

게임을 끄고 결과를 한 줄씩만 알려 주세요(예: "1 OK, 3 안 됨"). 로그는 Claude가 먼저 읽습니다.

- `Documents\My Games\Skyrim Special Edition\SKSE\CIGAR.log`
  - `[Observe] observing ... (first person)` → 1인칭 줌 시작
  - `[Observe] WARN the world FOV is ...` → 다른 모드가 줌을 덮어씀
  - `[Rest] pass time held (another device; ends on key up)` → 패드로 시작한 시간 보내기
  - `[Rest] pass time held (keyboard)` → 키보드로 시작한 시간 보내기

## 테스트 후 Claude가 할 일

1. `CIGAR.log`에서 위 네 줄과 WARN을 확인합니다. 실패한 항목이 있으면 로그로 원인을 찾아 고칩니다.
2. 모두 통과하면 넥서스 193080에 2.1.1 파일을 **Update**로 올립니다. 같이 할 일:
   - 설명 Requirements 절의 "Gamepads are not supported"를 패드 동작 문구로 바꿉니다.
   - 요약의 "Works with Skyrim alone"을 "No gameplay mods required"로 다듬습니다.
   - 태그에 **AI Media**를 추가합니다. AI-Generated Content는 이미 달려 있습니다.
   - 변경 내역: 1인칭 주시하기 줌, 패드 시간 보내기, FOV 덮어쓰기 경고.
3. `docs/031`, `docs/019`의 "Untested" 표시를 결과로 바꾸고 로컬 커밋합니다. GitHub push는
   사용자가 말할 때만 합니다.
4. MO2 파일을 건드렸다면 작업이 끝났을 때 MO2를 꺼진 상태로 둡니다.

## 결정이 남은 것

- **OStim:** 장면 중 프롬프트 숨김 판정에 OStim도 넣을지(넥서스 댓글 질문). 지금은 SexLab만 봅니다.
